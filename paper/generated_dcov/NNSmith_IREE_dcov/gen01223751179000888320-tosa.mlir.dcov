analysis res:
builtin.module
func.func
func.func is nested in builtin.module
tosa.negate
tosa.negate is nested in func.func
tosa.concat
tosa.concat is nested in func.func
tosa.sub
tosa.sub is nested in func.func
tosa.sub is connected with tosa.negate
tosa.sub is connected with tosa.concat
func.return
func.return is nested in func.func
func.return is connected with tosa.sub
